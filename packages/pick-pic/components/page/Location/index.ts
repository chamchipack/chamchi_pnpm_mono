type Position = {
  latitude: number;
  longitude: number;
};

type Center = {
  lat: number;
  lng: number;
};

type Markers = {
  _id: string;
  name: string;
  longitude: number;
  latitude: number;
  image?: string;
};
