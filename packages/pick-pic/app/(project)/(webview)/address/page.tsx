import Container from '@/components/page/Address/Container';
import { createNoMeta } from '@/config/utils/seo/seoNoMeta';

export const metadata = createNoMeta('주소찾기', '주소찾기 페이지입니다.');

const Page = () => {
  return <Container />;
};

export default Page;
